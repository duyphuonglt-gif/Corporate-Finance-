---
template: prompt-log
purpose: "Running log of meaningful AI prompts and outputs used in a project — supports reproducibility and AI-use disclosure"
audience: MBA academic
fields_required: [date, goal, prompt, tool, output_location, notes]
naming_convention: "prompt-log.md (one per project, lives in deliverables/)"
course: BUS-629
company: iQIYI Inc (IQ, NASDAQ)
stage: 4
---

# Prompt Log — BUS-629 iQIYI Project

**Author:** Luong Duy Phuong
**Course:** BUS-629 · University of Hawaiʻi at Mānoa, Shidler College of Business

| Date | Goal | Exact Prompt | Tool (LLM/Sheet/Code) | Output Link/Location | Notes |
|------|------|--------------|-----------------------|----------------------|-------|
| 2026-05-16 | Draft Stage 4 technical spec — Round 1 | "Before drafting, surface all assumptions you need to make. Then draft the full spec using the Stage 4 brief, spec template (fetched from GitHub), Stage 1 ratios template, and Stage 3 workbook." | Claude (Cowork, claude-sonnet-4-6) | docs/specs/2026-05-16-luong-iqiyi-spec.md | 4 pre-draft clarifications answered before any drafting: (1) audience = MBA academic; (2) benchmark = standalone FY2025 vs. FY2024, no peer comparison; (3) recommendation focus = management/operational; (4) D&A treatment = `INC_depreciation` is 0 because iQIYI embeds all D&A in COGS — Cash Coverage must use `CASH_depreciation_amortization` = 13,264,120 from CF Statement. 11 sections, 25+ ratios populated in a single pass. Round 1 baseline. |
| 2026-05-16 | HIL Test 1 — identify spec gaps via self-review + independent reconstruction | "Re-read the spec and identify all gaps before testing. [Separately, to fresh LLM with no prior context:] Reconstruct the ratio model from named ranges alone and produce a full analysis." | Claude (Cowork, claude-sonnet-4-6) + Claude (independent session, zero prior context) | deliverables/2026-05-16-luong-iqiyi-analysis-test.md | **Before/after note (most consequential gap: GAP 2).** Before: §7 validation table stated "Mismatch source: `currentYear_assets_total` vs. `startYear_equity` — methodological artifact, not an error." This was wrong — it conflated an assets variable with an equity variable. The actual mismatch is the same variable (equity) measured at two different year-end dates: `RATIO_leverage` divides by `currentYear_equity` (FY2025 balance sheet) while direct ROE divides `INC_net` by `startYear_equity` (FY2024 balance sheet). After: §7 rewritten to "RATIO_leverage uses `currentYear_equity` (FY2025 year-end) as its denominator, while direct ROE uses `startYear_equity` (FY2024 year-end). Both are internally consistent; the 3-basis-point difference is a methodological artifact, not a computation error." The spec caused this because the Du Pont mismatch explanation was drafted from the formula structure without verifying which year-end each named range actually referenced. The error was arithmetically invisible — both ROE values computed correctly at –1.56% and –1.53% — so only the independent reconstruction test surfaced it by showing the executor reproduce the wrong explanation verbatim. Seven additional gaps also identified: GAP C (executor wrote "RMB 478K" instead of "RMB 478M" — 3-order-of-magnitude error, no unit translation rule in §11), GAP 3 (FY2023 anchor outside model), GAP 4 (VIE recommendation missing), GAP B — spec listed Market-to-Book as `market_capitalization / currentYear_equity` without specifying that `market_capitalization` is in USD '000 while `currentYear_equity` is in RMB '000; dividing two different currencies directly produces a dimensionless ratio with no financial meaning (0.085x). Fix: added named range `market_capitalization_rmb = market_capitalization × fx_rate` = 1,136,292 × 6.9931 = 7,946,204 RMB '000 to §5 Derived Inputs; updated §6 formulas to `market_capitalization_rmb / currentYear_equity` = 0.597x (MVA = `market_capitalization_rmb − currentYear_equity` = –5,362,722 RMB '000); unit note rewritten to require FX conversion before any Performance ratio computation. Deferred: GAP 1, 5, A. |
| 2026-05-16 | Remove FY2023 to eliminate hallucination risk at root — Round 3 | "§8 still references FY2023 gross margin (27.5%) — a year absent from IS/CF tabs. Removing FY2023 entirely means the executor only touches data derivable from named ranges. Two-year comparison (FY2025 vs. FY2024) is sufficient for the rubric." | Claude (Cowork, claude-sonnet-4-6) | docs/specs/2026-05-16-luong-iqiyi-spec.md (Round 3) | **Before/after note (most consequential gap: GAP 3).** Before: §8 scope said "FY2023→FY2024→FY2025 where data available" and explicitly anchored profitability analysis to a FY2023 baseline gross margin of 27.5% — a figure that must be sourced from Form 20-F FY2023, which is outside the named-range model (the Stage 3 workbook IS/CF tabs contain FY2025 data only). After: all FY2023 references removed; §8 scope changed to "FY2025 vs. FY2024 year-over-year comparison only; do not reference FY2023 or any earlier period." Profitability baseline reset to: gross margin FY2024 24.9% → FY2025 21.1% = 380 bps compression. The spec caused this because the conditional "where data available" created structural ambiguity — each executor must guess which tabs hold FY2023 data, and any executor that encounters a FY2023 anchor figure will attempt to source it externally rather than flag the gap. Architectural decision: removing the year entirely is cleaner than adding a warning, because a warning still keeps the FY2023 percentage present and a sufficiently capable LLM will derive absolute figures from it. Removing the anchor eliminates the hallucination risk at the root. Simultaneously closed GAP A: Gross Margin was absent from §6 ratio table, so any executor adding it independently could use a different formula or label — formalized as `(INC_sales − INC_cost_goods_sold) / INC_sales` = 21.1% to lock formula and label across all executors. 6/8 gaps closed. |
| 2026-05-16 | HIL Test 2 — verify Round 3 fixes; identify new gaps — Round 4 | "Evaluate this test file against all identified gaps. Are there any new gaps?" | Claude (Cowork, claude-sonnet-4-6) + Claude (independent session, zero prior context) | docs/specs/2026-05-16-luong-iqiyi-spec.md (Round 4) | **Round-2 prompt.** Gap identified in Test 2: the executive summary cited "RMB 736.5M profit recorded in FY2024" — a figure absent from the named-range model. Traced the source: executor read "+2.7% net margin (FY2024)" from §1 Scope and back-calculated using FY2025 revenue (RMB 27,291.3M) as a proxy: 27,291.3M × 2.7% ≈ 736.9M. The spec caused this because §1 provides FY2024 percentages for reader context but never declares that FY2024 IS absolute figures are not in the model — any executor that sees a percentage will attempt to derive the underlying absolute if the spec does not explicitly prohibit it. Re-prompt applied to spec: "Add a blockquote at the top of §8 stating: IS and CF tabs contain FY2025 data only. FY2024 income statement absolute figures must not be derived or cited. Reference FY2024 profitability using only the margin percentages stated in §1. Do not back-calculate FY2024 absolute net income or revenue from these percentages." This simultaneously closed GAP D and the long-deferred GAP 5, which shared the same root cause: the spec never drew a boundary between percentages provided for context and figures derivable from named ranges. Also re-prompted to close GAP 1: "Move the CASH_depreciation_amortization declaration from §3 prose to §4 Named Range Conventions — an executor building the model from scratch looks at §4 to know which named ranges exist, not at §3 data tables." 9/9 gaps closed. |
| 2026-05-16 | HIL Test 3 — formula-by-formula verification + full 30-ratio audit | "Evaluate this test file. Audit every ratio formula and computed value against the Stage 3 workbook — must match exactly." | Claude (Cowork, claude-sonnet-4-6) + Claude (independent session, zero prior context) | docs/specs/2026-05-16-luong-iqiyi-spec.md (v2.0 confirmed) | **Before/after note (verification pass — no new gaps).** Test 3 performed explicit formula-by-formula reconstruction, more rigorous than Tests 1 and 2 — computing every ratio from named ranges and comparing against spec values before producing the analysis. All 9 previously identified gaps confirmed closed: `CASH_depreciation_amortization` used correctly (not `INC_depreciation` = 0), Du Pont mismatch explained as `currentYear_equity` vs. `startYear_equity`, no FY2023 or FY2024 absolute IS figures, VIE/currency/regulatory covered in Recommendation 5, all prose figures in RMB millions. Full 30-ratio audit then conducted against the Stage 3 workbook: every formula and computed value verified correct. Key confirmations: Market-to-Book = 0.085x (formula `market_capitalization / currentYear_equity`, no FX conversion per Stage 1 template convention); Cash Coverage = 14.83x using `CASH_depreciation_amortization` = 13,264,120. No spec changes required. Spec promoted to v2.0. Ready for Stage 5 execution. |
| 2026-05-16 | HIL Test 5 (Gemini) — cross-vendor executor test; verify spec is executor-agnostic — Round 5 | "Evaluate this Gemini-generated analysis against all spec gaps and ratio requirements." | Claude (Cowork, claude-sonnet-4-6) + Gemini (independent session, zero prior context) | docs/specs/2026-05-16-luong-iqiyi-spec.md (Round 5) | **Round-2 prompt.** Two gaps identified in Test 5 (Gemini). Gap 1 — GAP 2 re-triggered: the executive summary reproduced the original Round 1 error — "leverage uses `currentYear_assets_total` while direct ROA uses `startYear_total_assets`" — which §7 had correctly fixed in Round 2. Traced source: §9 instruction 2 still contained the Round 1 wrong language; §7 and §9 had diverged across rounds. Gemini resolved the contradiction by deferring to the later §9 instruction. Claude-family tests (Tests 1–4) masked this because Claude weighted the more detailed §7 over the briefer §9 — a different resolution heuristic than Gemini. Re-prompt applied: "Rewrite §9 instruction 2 to match §7 exactly: `RATIO_leverage` uses `currentYear_equity` (FY2025 year-end) as its denominator, while direct ROE uses `startYear_equity` (FY2024 year-end). The 3-basis-point difference is a methodological artifact, not a computation error. Do not treat as an error." Gap 2 — NEW GAP F: Gemini wrote the executive summary in Vietnamese, inferring output language from the author's name and file history. The spec had never stated a required output language. Re-prompt: "Add to §11 before the Tone instruction: Language: English only. All prose, headings, labels, and footnotes must be written in English regardless of the executor's default language." Cross-vendor testing was essential to expose both gaps: a spec that passes only Claude-family tests is not executor-agnostic. 10/10 gaps closed. |
